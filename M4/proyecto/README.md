# Proyecto acumulativo M4

Evoluciona M3 a persistencia real con JPA/Hibernate y H2 en desarrollo. Incluye entidades `Alumno`, `Curso` y `Auditoria`, repositorios Spring Data, Specifications, relaciones, transacciones y tests `@DataJpaTest`.
